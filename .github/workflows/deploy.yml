name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-typecheck:
    name: Lint & Type Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

      - name: TypeScript type check
        run: npm run typecheck

      - name: Build
        run: npm run build

  docker-build:
    name: Docker Build Validation
    runs-on: ubuntu-latest
    needs: lint-and-typecheck

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create dummy .env for Docker build
        run: |
          cp .env.example .env
          sed -i 's/=.*/=placeholder/' .env

      - name: Build Docker image
        run: docker build -t tikdismp-bot:test .

  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: [lint-and-typecheck, docker-build]
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy Placeholder
        run: |
          echo "Code quality checked, tests passed, and image built successfully!"
          echo "TODO: Add deployment steps (e.g., SSH to VPS, deploy to Render/Railway, etc.)"
          # Example for VPS via SSH:
          # - uses: appleboy/ssh-action@v1.0.0
          #   with:
          #     host: ${{ secrets.HOST }}
          #     username: ${{ secrets.USERNAME }}
          #     key: ${{ secrets.PRIVATE_KEY }}
          #     script: |
          #       cd /path/to/app
          #       git pull origin main
          #       npm ci
          #       npm run build
          #       pm2 restart aternos-bot

